# This file is a template. Copy it to .env and fill in actual values.

# Client Environment Variables (copy to client/.env)
VITE_API_URL=http://localhost:4000

# Server Environment Variables (copy to server/.env)
JWT_SECRET=your_secure_random_secret_here_change_in_production
NODE_ENV=development
PORT=4000
DATABASE_URL=sqlite:./data/database.sqlite